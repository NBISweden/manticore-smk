channels:
  - conda-forge
  - bioconda
  - default
dependencies:
  - multiqc
channels:
  - bioconda
  - conda-forge
  - defaults
dependencies:
  - fastqc ==0.11.9

channels:
  - bioconda
dependencies:
  - jellyfish
channels:
  - bioconda
  - conda-forge
  - defaults
dependencies:
  - picard ==2.22.1
  - snakemake-wrapper-utils ==0.1.3

channels:
  - bioconda
  - conda-forge
  - defaults
dependencies:
  - picard ==2.22.1
  - r-base ==3.6.2
  - snakemake-wrapper-utils ==0.1.3

channels:
  - bioconda
dependencies:
  - picard >=2.23
channels:
  - bioconda
dependencies:
  - qualimap
channels:
  - bioconda
dependencies:
  - sambamba
channels:
  - conda-forge
  - bioconda
dependencies:
  - bcftools ==1.11
channels:
  - bioconda
  - conda-forge
  - defaults
dependencies:
  - cutadapt ==3.4

channels:
  - bioconda
  - conda-forge
dependencies:
  - samtools ==1.10

channels:
  - bioconda
  - conda-forge
  - defaults
dependencies:
  - bwa ==0.7.17

channels:
  - bioconda
  - conda-forge
  - defaults
dependencies:
  - bwa ==0.7.17
  - samtools =1.12
  - picard =2.25

channels:
  - bioconda
dependencies:
  - pybedtools
channels:
  - bioconda
  - conda-forge
  - defaults
dependencies:
  - gatk4
channels:
  - bioconda
dependencies:
  - bcftools
  - gatk4
channels:
  - bioconda
dependencies:
  - bedtools
channels:
  - bioconda
dependencies:
  - samtools
channels:
  - conda-forge
  - defaults
dependencies:
  - perl
channels:
  - conda-forge
  - bioconda
  - defaults
dependencies:
  - perl
  - perl-text-nsp
